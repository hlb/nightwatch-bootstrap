# nightwatch boostrap template

Install node modules:

```
npm install
```

Run tests:

```
node_modules/nightwatch/bin/nightwatch -e chrome tests/demo
nightwatch -e phantomjs -t viewport-shots.js http://example.com
```
